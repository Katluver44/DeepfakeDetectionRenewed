# ─────────────────────────────────────────────────────────────────────────────
# Reproducibility environment for final_demo.ipynb  (Python 3.12)
#
# torch / torchaudio are the CUDA 12.9 builds. Install them FIRST from the
# PyTorch wheel index, then install the rest from PyPI:
#
#   pip install torch==2.8.0 torchaudio==2.8.0 --index-url https://download.pytorch.org/whl/cu129
#   pip install -r requirements.txt
#
# (CPU-only or a different CUDA: pick the matching index-url from
#  https://pytorch.org/get-started/locally/ — the pinned 2.8.0 version is what
#  the checkpoints and results were produced with.)
# ─────────────────────────────────────────────────────────────────────────────

# --- deep learning core (see note above for the torch index-url) ---
torch==2.8.0
torchaudio==2.8.0
pytorch-lightning==2.6.5
torchmetrics==1.7.4
transformers==4.57.6
einops==0.8.2

# --- datasets / model hub ---
datasets==5.0.0
huggingface-hub==0.36.0
pyarrow==21.0.0

# --- audio io ---
soundfile==0.14.0

# --- scientific stack ---
numpy==1.26.4
pandas==2.3.3
scipy==1.16.3
scikit-learn==1.7.2
matplotlib==3.10.9
tqdm==4.68.2
requests==2.32.3

# --- notebook execution ---
ipykernel==7.3.0
nbconvert==7.17.1
